const ws=new WebSocket((location.protocol==='https:'?'wss://':'ws://')+location.host+'/ws');
const view=document.getElementById('view'); let me=null,state=null,host=false,role=null,timerInterval=null;
const send=m=>ws.readyState===WebSocket.OPEN&&ws.send(JSON.stringify(m));
function esc(s){return String(s??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;','\\':'&#39;'}[c]))}
function rules(){return `<details class="notice"><summary><b>⚜ How to Play</b></summary><p><b>Goal:</b> Town eliminates every Mafia. Mafia wins when Mafia equal or outnumber the other living players.</p><p><b>Night:</b> Mafia choose a victim, Doctor protects someone, Detective investigates someone.</p><p><b>Dawn:</b> The narrator announces the death and reads the dead player's role and will.</p><p><b>Day:</b> Everyone discusses. Tap a player's profile to nominate them. The selected player is highlighted for everyone.</p><p><b>Trial:</b> The accused defends themselves, then everyone alive votes Guilty or Innocent. A Guilty majority eliminates them.</p><p><b>Wills:</b> Keep yours updated; it is read when you die. Your role stays private on your own device.</p></details>`}
function avatar(p){return p?.avatar?`<img class="avatar" src="${esc(p.avatar)}" alt="">`:`<div class="avatar dummy">${esc((p?.name||'?').slice(0,1).toUpperCase())}</div>`}
function profile(p,action=''){return `<div class="profile ${state?.nominee===p.id?'selected':''}" ${action?`onclick="${action}"`:''}>${avatar(p)}<div><b>${esc(p.name)}</b><small>${p.alive?'Alive':'Dead'}</small></div>${state?.nominee===p.id?'<span class="target">ON TRIAL</span>':''}</div>`}
function timer(){return state?.deadline?`<div id="timer" class="timer">30</div>`:''}
function startTimer(){clearInterval(timerInterval);if(!state?.deadline)return;const tick=()=>{const el=document.getElementById('timer');if(!el)return;const left=Math.max(0,Math.ceil((state.deadline-Date.now())/1000));el.textContent=left;el.classList.toggle('urgent',left<=10);};tick();timerInterval=setInterval(tick,250)}
function home(){view.innerHTML=`<div class="grid"><button class="btn" onclick="create()">Create Host Room</button><button class="btn" onclick="joinForm()">Join a Room</button></div>${rules()}`}
function create(){host=true;send({type:'create'})}
function joinForm(){view.innerHTML=`${rules()}<label>Room code</label><input class="input" id="code" maxlength="6" placeholder="6-character code"><label>Your name</label><input class="input" id="name" maxlength="24" placeholder="Your name"><label>Profile picture <span class="muted">optional</span></label><input class="input" id="photo" type="file" accept="image/*" onchange="previewPhoto()"><div id="photoPreview"></div><label>Your will <span class="muted">optional</span></label><textarea id="will" placeholder="Write something you want read if you die..."></textarea><button class="btn" onclick="join()">Enter Game</button>`}
let selectedAvatar='';
function previewPhoto(){const f=document.getElementById('photo')?.files?.[0];if(!f)return;const r=new FileReader();r.onload=()=>{selectedAvatar=r.result;document.getElementById('photoPreview').innerHTML=`<img class="avatar large" src="${esc(selectedAvatar)}">`};r.readAsDataURL(f)}
function join(){send({type:'join',code:document.getElementById('code').value,name:document.getElementById('name').value,will:document.getElementById('will').value,avatar:selectedAvatar})}
function hostLobby(){const enough=state.players.length>=4;const joinUrl=`${location.origin}/?room=${state.code}`;view.innerHTML=`${rules()}<div class="center"><p>Room code</p><div class="room">${state.code}</div><div class="qr">PLAYER LINK<br><small>${joinUrl}</small></div><p class="big">${state.players.length} / 4 minimum players</p><p class="muted">If the host also wants to play, open the player link on a private phone.</p></div><div class="profiles">${state.players.map(p=>profile(p)).join('')}</div><button class="btn danger" onclick="start()" ${enough?'':'disabled'}>${enough?'Start Game':'Need '+(4-state.players.length)+' more player'+(4-state.players.length===1?'':'s')}</button>`}
function hostView(){if(state.phase==='lobby'){hostLobby();return;}let title=state.phase==='role'?'Roles dealt — check your phones':state.phase==='night'?`Night ${state.night} — secret actions`:state.phase==='dawn'?'Dawn — reveal the result':state.phase==='discussion'?`Day ${state.day} — discussion`:state.phase==='trial'?'Trial — vote after the defense':'Game Over';view.innerHTML=`${timer()}<div class="notice center"><b>${title}</b><br><span class="muted">The host screen never shows secret roles.</span></div><div class="profiles">${state.players.map(p=>profile(p)).join('')}</div>${state.phase==='trial'&&state.nominee?`<div class="notice center">⚖️ <b>${esc(state.players.find(p=>p.id===state.nominee)?.name||'Player')}</b> is on trial.</div>`:''}${state.phase==='gameover'?`<div class="role">${state.winner==='town'?'🏘️ TOWN WINS':'🗡️ MAFIA WINS'}</div>`:''}${rules()}`}
function start(){send({type:'start'})}
function playerView(){if(state.phase==='lobby')return view.innerHTML=`${rules()}<div class="center">Room <b>${state.code}</b></div><div class="notice center">Waiting for the host to start…</div><h3>Players</h3><div class="profiles">${state.players.map(p=>profile(p)).join('')}</div>`;if(state.phase==='role')return view.innerHTML=`${timer()}<div class="role">${role?esc(role.toUpperCase()):'YOUR ROLE IS REVEALING…'}</div><button class="btn" onclick="showRole()">👁️ Show My Role</button><div id="roleHint" class="notice center hidden">Keep this screen private.</div>${willBox()}${rules()}`;if(state.phase==='night')return night();if(state.phase==='discussion')return discussion();if(state.phase==='trial')return trial();if(state.phase==='dawn')return view.innerHTML=`${timer()}<div class="notice center">Dawn…</div>`;if(state.phase==='gameover')return view.innerHTML=`<div class="role">${state.winner==='town'?'🏘️ TOWN WINS':'🗡️ MAFIA WINS'}</div>${rules()}`}
function showRole(){const h=document.getElementById('roleHint');if(h)h.classList.toggle('hidden')}
function willBox(){return `<label>Update your will</label><textarea id="will">${esc(state.players.find(p=>p.id===me)?.will||'')}</textarea><button class="btn" onclick="saveWill()">Save Will</button>`}function saveWill(){send({type:'will',will:document.getElementById('will').value})}
function choices(){return state.players.filter(p=>p.alive&&p.id!==me).map(p=>profile(p,`act('${p.id}')`)).join('')}
function act(id){send({type:'nightAction',target:id})}function night(){let title=role==='mafia'?'Choose your victim':role==='doctor'?'Choose someone to protect':role==='detective'?'Investigate someone':'Sleep…';return view.innerHTML=`${timer()}<div class="notice center"><b>NIGHT ${state.night}</b><br>${title}</div><div class="profiles">${['mafia','doctor','detective'].includes(role)?choices():'<p class="center muted">Waiting for the night to end…</p>'}</div>${willBox()}`}
function discussion(){return view.innerHTML=`${timer()}<div class="notice center"><b>DAY ${state.day}</b><br>Tap a profile to nominate them. Your selection is immediately visible to everyone.</div><div class="meeting">${state.players.filter(p=>p.alive).map(p=>profile(p,`nom('${p.id}')`)).join('')}</div>${state.nominee?`<div class="notice center">⚖️ <b>${esc(state.players.find(p=>p.id===state.nominee)?.name)}</b> is selected for trial.</div>`:''}${willBox()}`}
function nom(id){send({type:'nominate',target:id})}
function trial(){const n=state.players.find(p=>p.id===state.nominee);return view.innerHTML=`${timer()}<div class="notice center"><b>⚖️ TRIAL</b><br><span class="big">${esc(n?.name)}</span><br>Everyone can see who is being tried. Let them defend themselves.</div><div class="profiles">${n?profile(n):''}</div><button class="btn danger" onclick="vote('guilty')">Guilty</button><button class="btn" onclick="vote('innocent')">Innocent</button>`}function vote(v){send({type:'trialVote',vote:v})}
ws.onopen=()=>{const r=new URLSearchParams(location.search).get('room');if(r){joinForm();document.getElementById('code').value=r}else home()};ws.onmessage=e=>{const m=JSON.parse(e.data);if(m.type==='created'){state={code:m.code,phase:'lobby',players:[]};hostLobby()}if(m.type==='joined'){me=m.id}if(m.type==='role'){role=m.role}if(m.type==='state'){state=m.state;if(host)hostView();else playerView();startTimer()}if(m.type==='error')alert(m.message);if(m.type==='announcement')alert(m.text);if(m.type==='narration')console.log(m.text);if(m.type==='investigation')alert(`${m.name} is ${m.isMafia?'MAFIA':'NOT MAFIA'}`);if(m.type==='dawn'){alert(m.victim?`${m.victim.name} died. Role: ${m.victim.role}. Will: ${m.victim.will||'(none)'}`:'Nobody died last night.')}};
